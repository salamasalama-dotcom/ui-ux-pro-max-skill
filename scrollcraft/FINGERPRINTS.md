# FINGERPRINTS

One row per shipped build. Append only. A new build must differ from **every**
existing row on at least 4 of the 6 dimensions, checked against each row
individually. If a planned build fails the gate, change the plan, not this file.

| # | Grammar | Nav treatment | Hero device | Act-sequence shape | Close pattern | Signature move | World | Port |
|---|---|---|---|---|---|---|---|---|
| 1 | Live surface | Three-edge app chrome: status bar, module sidebar with live counts, day rail. No wordmark-plus-CTA bar. | The console already mid-conversation at 08:12, one message in and the assistant typing. No title, no media. | pin 2.4 / flow / pin 2.2 / flow / pin 3.4 (peak) / flow / pin 1.4 · 7 acts · ~12.6vh | First-run form inside the pinned stage, computing a live setup preview, footer in the same stage so there is no dead tail | The scrollbar is a clinic day: scroll position is clinic time, the day rail is that clock and is draggable both ways, and every event the day passes is stamped permanently onto it | None (no photography; the operable console is the imagery) | `projects/klinikos-live-surface` |
